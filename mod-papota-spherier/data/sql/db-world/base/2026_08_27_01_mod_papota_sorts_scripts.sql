-- Accroches des scripts C++ aux sorts de classe — FICHIER NEUTRALISÉ.
--
-- Il posait à la main les lignes de `spell_script_names` pour la plage
-- 8600000-8600093. Depuis, `gen_sorts_classes.py` les écrit lui-même dans
-- 2026_08_27_00_mod_papota_sorts_classes.sql, à partir de la table de vérité.
--
-- LE GARDER TEL QUEL ÉTAIT UN PIÈGE DORMANT : il s'applique APRÈS le fichier
-- généré (l'ordre est alphabétique, _01_ suit _00_) et son DELETE couvrait
-- toute la plage. Il n'était plus rejoué — son empreinte ne changeait pas —
-- mais la moindre retouche l'aurait réveillé et il aurait écrasé les
-- accroches générées par les siennes, périmées : il nommait encore
-- `spell_papota_charge_sauvage` (8600090) et `spell_papota_pleine_lune`
-- (8600092), deux scripts supprimés depuis.
--
-- Vidé plutôt que supprimé : l'updater d'AzerothCore suit les fichiers par
-- leur nom, et un fichier qui disparaît le fait râler.
